# Application Integration

Applications are represented by an Application Registry containing discovered metadata, adapter, supported actions, permissions, health and current state.

Adapters are capability-based. An application with a strong API should not be driven through fragile GUI automation. GUI and vision are fallbacks, not the default.

Application metadata is untrusted data. Discovery does not grant permission to execute actions.
